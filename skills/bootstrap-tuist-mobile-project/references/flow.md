# Bootstrap Tuist Mobile Project Flow

1. **Capability detection** – Check `git`, `gh`, `gh auth status`, `mise`, `tuist`, and `tuist auth whoami`. Each capability is either available, installed but not authenticated, or missing. The summary drives the next section, but it must not hide any of the supported modes.
2. **Mode selection** – Ask the user to choose between `local-only`, `github-backed`, and `github-and-tuist-cloud`. If a capability is missing, surface the block and ask whether to postpone the mode or fix the tooling. The skill never downgrades silently and never filters out blocked modes.
3. **Template selection** – Ask whether to scaffold the pure iOS template or the iOS + Catalyst template. Keep template aliases stable so the CLI payload becomes deterministic.
4. **Metadata collection** – Ask questions in this order: project name; destination path; GitHub owner only when the selected mode is `github-backed` or `github-and-tuist-cloud`; repo name, where any default derived from the project name must still be explicitly confirmed by the user; repo visibility only in those GitHub-backed modes; bundle identifier; default iOS simulator device; then, when the selected mode is `github-and-tuist-cloud` and the current capability state supports it, ask separately whether to create the Tuist Cloud project and whether to run `tuist setup cache`; finally ask about initial commit and push choices.
5. **Optional overrides** – `full_handle` is derived from the confirmed owner and repo name by default. Only surface it as an override when the user needs to bind to a specific existing handle or correct the derived value. `cache_service_slug` is derived from that handle by default and belongs in the JSON handoff, not in the default Task 1 interview.
6. **Fail-closed directory branch** – Before local scaffolding, if the target directory already exists, stop and ask the user to choose `reuse`, `replace`, or `abort`.
7. **Fail-closed repo-name branch** – Before GitHub repo creation, if the requested repo name is already taken, stop and ask whether the user wants to choose another repo name. If they decline, force an explicit choice to switch modes or abort.
8. **Fail-closed full_handle branch** – Before Tuist Cloud creation, if the requested `full_handle` already exists, stop and ask whether the user wants to bind to that handle or choose another handle.
9. **Confirmation checkpoint** – Explicitly confirm GitHub repo creation with the chosen visibility, Tuist Cloud project creation, `tuist setup cache`, initial commit creation, and push. No action executes without a yes/no answer.
10. **GitHub execution** – If confirmed and the chosen mode is `github-backed` or `github-and-tuist-cloud`, the orchestration layer should run `gh repo create` with the requested visibility. Do not treat this as permission to push later steps automatically.
11. **Local scaffolding** – Resolve the destination directory, prepare a local template source path, ensure any existing target directory is handled only through the explicit `reuse`, `replace`, or `abort` branch, and in later tasks invoke `bin/zach-mobile-init --config <path-to-json-config>` from the Zach-Skills repo root. The future CLI consumes the prepared local template source and performs local placeholder mutation only.
12. **Cloud and cache** – When Tuist Cloud creation is separately confirmed, the orchestration layer should run `tuist project create <full_handle>`. When cache setup is separately confirmed, it should run `tuist setup cache` afterward.
13. **Warm and verify** – Treat `mise run warm-external-cache` as the default local post-init setup/verification step.
14. **Commit** – If the user confirmed initial VCS setup, the orchestration layer should run `git add` and `git commit` only after the warm/generate flow completes.
15. **Push and final output** – Push only if the user explicitly confirmed push. Then present the resulting project path, repo URL when applicable, and the ready-to-use commands for run/build/test/share recorded in the template flows.

`scripts/bootstrap_mobile_project.py` is the current shell helper surface for capability detection, strongly typed approvals, and payload assembly. The CLI already exists; later tasks will implement the full interview/orchestration layer around it.
